# [功能或候选版本] 人工验收

> 编写提示，成稿删除：先读作者认可的已有手册，优先沿用它的样式；本模板只补缺项，不强制覆盖已有结构。小改动可以只保留准备、一张操作表、停止和反馈。保留“做 / 看 / 记”、必要判定与恢复，删除无关章节和占位符。正常路径不穿插工程自检或故障诊断。

本文验证[本次候选及变更]，预计[主动操作时长；必要长时间等待另列]。本次必验为[分组]；[专项]属于[本阶段门槛或计划已明确的后续范围]。当前人工结果为[作者已报告的事实；首次交付写未执行]。

## 1. 准备和打开

[已有环境如何确认并继续，或如何从已交付入口启动。GUI 可用时直接写点击步骤；必须用命令行时只给必要的一小段，并说明工作目录、前提、可能影响、等待多久、成功提示和页面地址。已有入口包含编译时不要再列独立编译步骤。不要让作者先检查 SDK、Git、文件哈希和所有配置。]

[需要作者保留的本次环境标识，例如自动生成的清单路径；Agent 在文末注明候选及核实依据，不要求作者重新做同一套核查。入口缺失时明确受影响案例与所需补充，不虚构步骤。]

[本次会改变的测试资源、模拟或故障注入范围及恢复入口；仅写实际相关的风险。复用同一环境，只有确需隔离时才要求重新准备。]

## 2. 页面上先认清这几处（需要时保留）

| 页面显示的名称 | 怎么用、怎么看 |
|---|---|
| [真实按钮或显示] | [操作对象、成功表现及必要注意点] |
| [查询或状态显示] | [从哪里看本轮结果；已有页面可观察时不要再配命令查询] |

[只解释本次判定必需的状态。例如已接受不等于完成、当前版本与目标版本不同、切换对象是否同步输入框。步骤中已经说明清楚的内容不重复列。]

## 3. 分组操作

### 第一组：[要验证的用户行为]

前提：[本组对象、必要初始状态；需要比较增量时从页面记录 N]。

| 步骤 | 做什么 | 看到什么并记录 |
|---|---|---|
| 1-1 | [选择真实控件值，点击真实按钮一次] | [明确提示或变化，保存必要截图/编号] |
| 1-2 | [下一步真实操作；异步时写查询入口、间隔、上限和停止条件] | [终态及必要副作用；未能判定时应保留什么] |
| 1-3 | [只在需要时刷新或复查] | [操作后值 M，与基线比较 M-N；不要默认总数从零开始] |

通过要求：[必要预期同时成立]。若[前提成立时的契约违背]，记失败并保留首次现场；[外部条件]导致无法判断才记受阻。只完成部分步骤且无明确违约或外部阻断时，记部分执行。

[涉及异步时：查询安排的依据、人工观察上限与产品期限的区别。只到人工观察上限仍无终态，不直接判产品失败。超时或取消不自动表示底层工作已退出；需要时列同一执行编号的迟到结果与副作用复查。]

[本组必要恢复；无则删除。不要在每组重复整套状态定义、取证清单或退出命令。]

### 第二组：[负向或其它独立行为，按需复制操作表]

[明确预期拒绝、报错、超时或取消就是本案例的正确结果；仍需核对禁止的副作用没有发生。组数按风险裁剪，不按源码文件罗列。]

## 4. 停止和恢复

1. [通过现有界面停止本次任务，解除本次注入；正常步骤必须可直接照做。]
2. [通过实际交付入口退出本次宿主，写明如何确认已退出；关闭浏览器是否等于停服务须说清楚。]
3. [保留本次证据的简单方法；需要清理时限定本次资源，写明清理入口。没有清理入口时由已有授权的 Agent 处理或明确缺口，不能默认让作者编写清理脚本。]

[未结束的任务或异常现场按实际情况处理，不批量关闭共享进程或删除无关数据。]

## 5. 反馈结果

- **通过**：作者明确报告全部必要预期均满足。
- **失败**：前提成立且实际结果违反已确认的契约；步骤未做完也不能降为部分执行。
- **受阻**：外部条件导致无法判断，且尚无本案例违约证据；不要将待验的产品故障归为受阻。
- **部分执行**：已经开始但证据不足，且无明确违约或外部阻断；保留完成步骤和待补项。
- **未执行**：尚无作者执行证据；自动化通过或 Agent 复核不能代替。

只需回报组号与结果；有异常或部分完成时补充下面信息。截图已包含的字段无需逐项抄写，长编号按字符串原样保留。

    本次环境/候选：
    组号及已做步骤：
    结果：
    异常发生在哪一步、实际看到什么：
    截图或证据位置：
    是否已正常停止、还有什么未做：

## 6. 仅遇到问题时（没有必要诊断则整节删除）

**仅当[明确故障现象]时使用。正常验收跳过。**

[先给已有界面或日志文件的查看路径；确需命令时写明目的、最少参数、只读/副作用边界和预期关键输出。不要把大量排障步骤交给作者；无法直接恢复时保留现场交由 Agent 排查。]

## 核实依据与证据边界（简短，供追溯）

[Agent 已核实的源码/界面/脚本及候选依据，判定所据需求或契约；未实跑要说明。无需作者再执行同一套核查。列明模拟替代环节及无法证明的真实设备、发布等范围。]

[只有确有旧证据或换候选时，写明受影响的重验项及保留旧证据的依据。不得自行延后必验专项，不把旧候选或间接证据写成当前人工通过。]
