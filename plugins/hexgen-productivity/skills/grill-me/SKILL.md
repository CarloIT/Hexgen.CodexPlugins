---
name: grill-me
description: 通过追问澄清计划或设计，跟踪 Matt Pocock 的上游 Grill Me；当前按轮次提出所有依赖已满足的问题，适用于希望集中回答的追问会话。
---

读取并遵循随本技能提供的[上游追问规则](references/上游追问规则.md)，开始本次追问会话。

这里的 `grilling` 指向该相对路径，不调用其它插件或个人目录中的同名技能。引用文件保留上游原文；`name: grilling` 是其来源标识，不是第三个技能入口。

Codex 适配：上游入口的 `Skill` 工具调用由上述文件读取替代。需要查证事实时按上游要求使用可用的子 Agent；若当前环境不提供子 Agent，则自行查证，仍按依赖关系组织问题，不把可查询的事实交给用户。
